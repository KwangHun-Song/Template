namespace DrawLine {
    public enum GameResult { None, Clear, Stop }
    public enum InputType { None, Down, Up }
    public enum ColorIndex { None, Red, Orange, Yellow, Green, Blue, Navy, Purple, Brown, Pink, Cyan }
    public enum Direction { None, Right, Down, Left, Up }
}